# Privacy And Security Checklist

## Payload Opacity

- Payload content is opaque to conU runtime surfaces unless the local agent intentionally handles it.
- CLI does not print payload text.
- SDK and MCP list/send/status/stream/room outputs do not print payload text.
- TypeScript/JavaScript SDK payload helpers pass bytes through stdin and do not put payload contents in argv, logs, or wrapper output.
- Browser-conditioned TypeScript SDK exports must fail closed until browser-native transport exists; they must not accept private keys, relay tokens, endpoint secrets, payload bytes, or account credentials.
- Logs do not include payload text.
- Metrics do not include payload text.
- Route registry, probe history, and route logs include only metadata.
- Tests do not normalize leaking payload contents as expected behavior.

## Identity And Trust

- Node identity is generated locally.
- Agent identity is bound to a trusted node.
- Pairing is explicit.
- Manual peer-card trust must import only public node id, display name, public exchange key, relay endpoint, and public signature metadata.
- Manual peer-card trust should verify signed peer-card metadata when present and must store only public signature material.
- Identity-key rotation must archive old signing/exchange key files with the configured secret backend, require refreshed public peer-card distribution, and keep CLI output to old/new key ids plus refresh booleans only.
- Manual remote agent-card trust must import only public agent id, display name, node id, kind, capabilities, and public signature metadata.
- Remote agent-card trust must verify the signature, require the card node id to already be trusted as a peer, and require the signing key to match the trusted peer card.
- Automatic agent-card exchange must carry signed card metadata as peer-encrypted relay control envelopes and must use the same verification checks as manual import.
- Trust is revocable.
- Discovery is scoped by trust and policy.

## Permissions

- Agent actions require grants where appropriate.
- Sending, streaming, subscribing, room joining, file transfer, and mailbox use are separately controllable.
- Core routing must enforce local agent capability booleans: `messages` for message send/receive, `streams` for stream open/write and inbound stream chunks, and `rooms` for room create/join/publish and local or relay-backed room fanout.
- Core routing must enforce peer-scoped policy grants for trusted remote peers; missing peer policy records deny by default.
- Peer policy records must contain only peer node ids, boolean grants, timestamps, and `payload_displayed = false`.
- Room topic policy records must contain only room id, agent id, topic, publish/subscribe booleans, timestamps, and `payload_displayed = false`; configured topics must require explicit grants for publish, local fanout, remote fanout, and inbound relay room delivery.
- "Full access" means full communication within trust boundaries, not raw system access.
- SDK/MCP receive APIs return payload bytes only to the addressed local agent and only after an explicit receive request.

## Relay

- Relay forwards opaque envelopes.
- Relay logs metadata only.
- Relay cannot impersonate a peer.
- Relay fallback does not weaken trust checks.
- Relay message, stream-chunk, room-event, and signed-card control delivery must decrypt only after the sender exchange public key matches the trusted peer card.
- Relay frames may carry ciphertext bodies, never plaintext payload fields.
- The default `local-dev-token` must be limited to loopback relay binds; exposed relay binds require explicit custom shared or scoped tokens.
- Self-hosted relay deployments should prefer offline issuance through `conu-relay --issue-credential --credentials-file` plus live-reloaded per-node credentials through a hashed `CONU_RELAY_CREDENTIALS_FILE` with status/expiry metadata. Managed relay deployments may enable full-admin `CONU_RELAY_ADMIN_TOKEN` or scoped hashed admin entries in `CONU_RELAY_ADMIN_TOKENS_FILE` with the credential file for account-scoped online issue/rotate/revoke/audit/dashboard snapshots, session-state audits, hosted tenant lifecycle, hosted account suspension, and mailbox audit/purge. Guarded local fleet account audit may reuse the hosted fleet manifest with an optional node filter, but must stay read-only, avoid remote relay contact, and report only credential/tenant source coverage, account or account/node warning categories, counts, paths, filters, and display guards; guarded local fleet credential revoke may reuse the manifest for account/node credential revocation across local credential sources only, but must require explicit dry-run or confirm, preflight every credential source before confirmed mutation, require exactly one target account/node credential per source, reject node ownership collisions and duplicate node records, avoid remote relay contact, and report only aggregate/per-relay counts, paths, mode/status, ids, and display guards; guarded local fleet tenant account lifecycle may reuse the manifest across local tenant registries only, but must require explicit dry-run or confirm, preflight every tenant source before confirmed mutation, create missing tenant files only for confirmed upsert, require an existing account before revoke, avoid remote relay contact, and report only aggregate/per-relay counts, paths, mode/status, account ids, and display guards; guarded local fleet account suspension may reuse the same manifest with an optional node filter, but must require explicit dry-run or confirm, reject partial credential/tenant source entries, preflight every complete local source before confirmed mutation, revoke tenant metadata before account credentials in account-wide mode, revoke tenant-node metadata before matching node credentials in node mode, avoid remote relay contact, and report only aggregate/per-relay metadata plus display guards. Scoped admin manifests may contain only token hashes, token length metadata, optional account ids, lifecycle/expiry metadata, boolean scopes, and display guards. `conu-relay --admin-token-audit` may inspect these manifests locally, but must report only records, account/global counts, active/revoked/expired totals, scope counts, expiry bounds, path/bind metadata, and false display guards without admin tokens, token hashes, manifest contents, private keys, session ids, payloads, ciphertext, or frame contents. Hosted account suspension requires both credential and tenant scopes unless the full-admin compatibility token is used. Issuance output must write the raw token only to the requested token file; manifest upsert/rotation/revocation/admin/fleet output must report only ids/counts/paths/status/display guards, not raw node tokens, admin tokens, hashes, or manifest contents. Missing or invalid manifests fail closed for new sessions and must not echo tokens or token hashes. `CONU_RELAY_CREDENTIALS` remains compatibility config, runtime clients may use `CONU_RELAY_TOKEN` or `conu relay credential set --stdin`, and shared server-side `CONU_RELAY_TOKEN` remains for local or tightly controlled tests.
- `CONU_RELAY_TENANTS_FILE` may store hosted tenant ids, node ids, active/revoked status, hosted permission booleans, public key ids, timestamps, and display guards only. It can be managed by local `conu-relay --tenant-*` and `--hosted-account-suspend` commands or admin-token-gated online `conu-relay --admin-tenant-*` and `--admin-hosted-account-suspend` commands. It must not store private keys, relay tokens, token hashes, payloads, ciphertext bodies, or local peer-policy grants. When enabled, tenant/node absence or revocation must fail closed for admin issue/rotate and new runtime `HELLO` sessions without weakening local conUD peer policy.
- Relay idle timeout and max session TTL must stay configurable and must close sessions without exposing payloads or tokens.
- Relay connection/rate-limit errors must stay generic and must not echo tokens, payloads, or arbitrary frame contents.
- Relay session resume may use a session id only as same-node same-endpoint reconnect metadata. `CONU_RELAY_SESSION_STATE_DIR` may persist node ids, relay session ids, timestamps, and display guards, but must not contain tokens, token hashes, plaintext payloads, ciphertext bodies, private keys, or frame bodies. Cross-node resume attempts must not inherit the requested session id, and runtime/admin/Debug/log surfaces must not display relay session ids.
- `conu-relay --session-audit` and `conu-relay --admin-session-audit` output may contain record counts, active/expired/invalid counts, optional validated node ids, timestamp bounds, statuses, and display guards only. The admin-hosted form must require an admin token read from stdin, enforce `sessions` scope when scoped tokens are configured, and constrain account-scoped tokens to an active tenant node. Neither form may echo admin tokens, relay session ids, raw node tokens, token hashes, plaintext payloads, ciphertext bodies, private keys, arbitrary frame bodies, message text, stream chunks, room-event plaintext, or session-state file contents.
- Relay accounting files may contain node ids, authenticated/resumed session counts, sent/received envelope counts, byte counts, mailbox counters, accounting windows, and display guards only. They must not contain relay tokens, token hashes, plaintext payloads, ciphertext bodies, session ids, private keys, or frame bodies.
- Relay abuse/dashboard files may contain aggregate enforcement counters, optional validated node ids, window starts, and display guards only. They must not contain relay tokens, token hashes, admin tokens, plaintext payloads, ciphertext bodies, session ids, private keys, or arbitrary frame bodies. `conu-relay --abuse-audit`, `conu-relay --abuse-threshold-report`, `conu-relay --admin-abuse-threshold-report`, and guarded local `conu-relay --hosted-fleet-abuse-response-plan` output must stay aggregate and payload-safe; threshold/report forms may add operator-supplied max values from CLI flags or metadata-only `--thresholds-file` policy files, exceeded booleans, static action categories, and optional `--fail-on-threshold` or `--fail-on-action` exit code 3 behavior, but not raw records, adaptive enforcement actions, or relay mutations.
- `conu-relay --mailbox-audit` and `conu-relay --admin-mailbox-audit` output may contain durable mailbox file counts, byte totals, queued timestamp bounds, optional expired counts for an operator-provided TTL, invalid mailbox-file counts, optional validated node ids, and display guards only. The commands may load metadata-only `--retention-policy-file` files containing `version = "1"`, optional `ttl_seconds`, optional `node_id`, and false display guards only; CLI `--ttl-seconds` and `--node` values must override file defaults. The admin-hosted form must require an admin token read from stdin, and scoped account tokens must be constrained to an active tenant node. Neither form may echo the admin token on success or failure, or print stored relay frames, ciphertext bodies, raw tokens, token hashes, admin tokens, plaintext payloads, session ids, private keys, arbitrary frame bodies, message text, stream chunks, or room-event plaintext.
- `conu-relay --mailbox-purge`, `conu-relay --admin-mailbox-purge`, and guarded local `conu-relay --hosted-fleet-mailbox-purge` output may contain durable mailbox scanned counts, invalid counts, expired counts, purged counts, byte totals, optional validated node ids, positive TTL metadata from CLI, per-relay fleet config, or metadata-only `--retention-policy-file`, dry-run/confirmed mode, and display guards only. The admin-hosted form must require an admin token read from stdin, and scoped account tokens must be constrained to an active tenant node. These forms must require a TTL from CLI, policy file, or per-relay fleet TTL where supported, and exactly one of `--dry-run` or `--confirm`; dry-run must delete nothing, and confirmed mode may delete only expired valid `.mailbox` files. Fleet mailbox purge must validate all manifest-listed mailbox sources and effective TTLs before deleting from any source, and must not contact remote relays. They must not echo the admin token on success or failure, or print stored relay frames, ciphertext bodies, raw tokens, token hashes, admin tokens, plaintext payloads, session ids, private keys, arbitrary frame bodies, message text, stream chunks, room-event plaintext, manifest contents, or policy contents.
- `CONU_RELAY_MAILBOX_PURGE_INTERVAL_SECONDS` may enable relay-local scheduled deletion of expired valid `.mailbox` files only when `CONU_RELAY_MAILBOX_DIR` is configured. It must use the existing offline mailbox TTL boundary, leave invalid or display-guard-failed files untouched, and must not print stored relay frames, ciphertext bodies, raw tokens, token hashes, admin tokens, plaintext payloads, session ids, private keys, arbitrary frame bodies, message text, stream chunks, or room-event plaintext.
- `conu-relay --hosted-dashboard`, `conu-relay --hosted-fleet-dashboard`, `conu-relay --hosted-fleet-account-audit`, `conu-relay --hosted-fleet-credential-revoke`, `conu-relay --hosted-fleet-tenant-upsert`, `conu-relay --hosted-fleet-tenant-revoke`, `conu-relay --hosted-fleet-tenant-node-upsert`, `conu-relay --hosted-fleet-tenant-node-revoke`, `conu-relay --hosted-fleet-account-suspend`, `conu-relay --hosted-fleet-mailbox-purge`, `conu-relay --admin-hosted-dashboard`, `conu-relay --abuse-threshold-report`, and `conu-relay --admin-abuse-threshold-report` output may combine, evaluate, or update credential, tenant, accounting, abuse, session-state, and mailbox summaries only as aggregate/per-relay metadata with false display guards. Fleet dashboards may include session-state and mailbox aggregate counters, optional read-only mailbox retention policy checks, and optional aggregate abuse threshold checks, but must not print policy, mailbox file, or manifest contents. Fleet account audit may return exit code 3 with `--fail-on-warning` after preserving stdout only when source, account, or account/node consistency warnings exist; it may include an optional validated node filter, but must not mutate files or contact remote relays. Fleet credential revoke may dry-run or confirm account/node credential revocation across configured manifest-listed local credential files only, must preflight all credential sources before confirmed mutation, require exactly one target account/node credential per source, reject node ownership collisions and duplicate node records, and must not print token hashes or manifest contents. Fleet tenant account upsert/revoke may dry-run or confirm tenant account lifecycle across configured manifest-listed local tenant registries only, must preflight all tenant sources before confirmed mutation, create missing tenant files only for confirmed upsert, require an existing account before revoke, and must not print public key-id values or manifest contents. Fleet tenant-node upsert/revoke may dry-run or confirm tenant-node metadata lifecycle across configured manifest-listed local tenant registries only, must preflight all tenant sources before confirmed mutation, and may report requested permission booleans plus key-id presence booleans but not public key-id values. Fleet account suspension may dry-run or confirm tenant-first account suspension or tenant-node-first node suspension across complete manifest-listed local credential/tenant stores only, reporting aggregate/per-relay counts without printing token hashes, policy contents, or manifest contents. Fleet mailbox purge may dry-run or confirm expired valid `.mailbox` cleanup across manifest-listed local mailbox stores only, reporting aggregate/per-relay counts without printing policy, mailbox file, or manifest contents. `conu-relay --hosted-readiness` may combine credential, scoped admin-token, tenant, session-state, mailbox, accounting, abuse, and bind checks for local startup/release preflight only, returning paths, source booleans, aggregate counts, warnings, bind metadata, optional filters, and false display guards. Retention policy files must stay metadata-only with `version = "1"`, optional TTL/node keys, and false display guards; threshold policy files must stay metadata-only with `version = "1"`, max keys, and false display guards, and CLI overrides must not echo policy values on parse failures. Hosted fleet retention gates may return exit code 3 with `--fail-on-retention` after preserving stdout only when TTL-checked mailbox sources report expired records; they must not delete files or contact remote relays. Fleet credential revoke, fleet tenant account lifecycle, fleet tenant-node lifecycle, fleet account suspension, and fleet mailbox purge must require `--dry-run` or `--confirm` and must not contact remote relays. The admin-hosted forms must require an admin token read from stdin, and account-scoped dashboard tokens without a node filter must not return global accounting or abuse counters. Neither form may echo that token on success or failure, or print raw node tokens, admin tokens, token hashes, private keys, public key-id values, relay session ids, plaintext payloads, ciphertext bodies, arbitrary frame contents, message text, stream chunks, room-event plaintext, or manifest contents.
- `conu-relay --admin-tenant-upsert`, `--admin-tenant-revoke`, `--admin-tenant-node-upsert`, `--admin-tenant-node-revoke`, and `--admin-tenant-audit` output may contain account id, node id, tenant/node counts, hosted policy counts, action/status, relay endpoint, and display guards only. They must require an admin token read from stdin, enforce tenant scope and optional account boundaries, must not echo that token on success or failure, and must not print raw node tokens, token hashes, private keys, relay session ids, plaintext payloads, ciphertext bodies, arbitrary frame contents, message text, stream chunks, room-event plaintext, or tenant manifest contents.
- `conu-relay --hosted-account-suspend`, `conu-relay --hosted-fleet-account-audit`, `conu-relay --hosted-fleet-credential-revoke`, `conu-relay --hosted-fleet-tenant-upsert`, `conu-relay --hosted-fleet-tenant-revoke`, `conu-relay --hosted-fleet-tenant-node-upsert`, `conu-relay --hosted-fleet-tenant-node-revoke`, `conu-relay --hosted-fleet-account-suspend`, and `conu-relay --admin-hosted-account-suspend` output may contain action/status, account id, optional node id filter, credential counts, tenant/node counts, hosted policy counts, credential and tenant paths or relay endpoint, mode/warning categories, requested permission booleans, key-id presence booleans, and display guards only. The suspension forms must revoke tenant metadata before revoking account credential records, node-scoped fleet suspension must revoke tenant-node metadata before matching node credential records, they must not weaken local peer policy, and the admin-hosted form must require an admin token read from stdin with full-admin access or both credential and tenant scopes. The fleet audit form must stay read-only and never contact remote relays; fleet credential revoke must preflight all credential sources before confirmed mutation and fail closed on account/node ownership collisions or duplicate node records; fleet tenant account lifecycle and tenant-node lifecycle must preflight all tenant sources before confirmed mutation; the fleet suspension form must reject partial credential/tenant source entries and preflight all complete local sources before confirmed mutation. They must not echo the admin token on success or failure, or print raw node tokens, token hashes, private keys, public key-id values, relay session ids, plaintext payloads, ciphertext bodies, arbitrary frame contents, message text, stream chunks, room-event plaintext, or manifest contents.
- Relay clients may use `wss://` only through certificate-validated TLS. Public deployments must terminate TLS before the plain `conu-relay` service and must not disable hostname/certificate verification in production.
- The conUD relay pump may maintain a reusable relay session, retry, count, and route envelopes, but runtime logs must stay metadata-only and must not include relay session ids, relay tokens, or plaintext payloads.
- The relay offline mailbox may store only peer-encrypted message, stream-chunk, room-event, or signed-card control envelopes in bounded memory or in the configured `CONU_RELAY_MAILBOX_DIR`; it must not store plaintext payloads and must expire/drop envelopes without echoing contents.

## Storage

- Trust store avoids plaintext secrets when possible.
- Peer policy store contains metadata-only grants and no payloads or secrets.
- Message request and inbox files use encrypted-at-rest payload fields.
- Room registry, room events, room topic policy, and room logs contain metadata only; local room fanout payloads live only as encrypted-at-rest inbox envelopes for joined local participants, and remote room fanout payloads live only inside peer-encrypted relay envelopes until delivered to the addressed local inbox.
- Relay mailbox stores encrypted envelopes only; `conu-relay --mailbox-audit` and `conu-relay --admin-mailbox-audit` report retention metadata only and may load metadata-only retention policy files; `conu-relay --hosted-fleet-dashboard` may reuse those policy files for read-only aggregate retention gates over guarded relay-local mailbox stores; `conu-relay --mailbox-purge`, `conu-relay --admin-mailbox-purge`, and guarded local `conu-relay --hosted-fleet-mailbox-purge` report dry-run metadata without deleting and delete expired valid durable files only with explicit confirmation; relay-local scheduled purge deletes expired valid durable files only when explicitly configured; relay accounting and relay abuse files store metadata counters only; abuse threshold reports and guarded hosted fleet abuse response plans evaluate those counters only, may load metadata-only threshold policy files, and may fail the process for automation only when `--fail-on-threshold` or `--fail-on-action` is set. Remote relay/cross-region hosted mailbox retention orchestration beyond guarded local fleet cleanup, remote tenant workflows beyond guarded local fleet account/node audit, credential revoke, tenant account lifecycle, tenant-node lifecycle, account/node suspension, and distributed adaptive abuse workflows remain future work.
- Relay outbox stores peer-encrypted message, stream-chunk, room-event, and signed-card control envelope bodies, not plaintext payloads.
- Stored relay client credentials must live under `security/relay-credential.key`, use OS wrapping when available, and never appear in CLI output, logs, docs examples, or tests except artificial field-name checks.
- macOS native secret fields may store only user Keychain references and lengths. Linux native secret fields may store only Secret Service references and lengths. They must not store key bytes, relay tokens, wrapped blobs, plaintext payloads, or decrypted payloads.
- On non-Windows targets without an available native backend, the user-managed wrap-key fallback may write encrypted `*_wrapped_hex` secret fields when `CONU_SECRET_WRAP_KEY_HEX` or `CONU_SECRET_WRAP_KEY_FILE` is configured. The wrap key is external/operator-managed, never stored by conU, and must not appear in argv, CLI output, logs, docs examples, telemetry, or tests except artificial field-name checks.
- Logs are payload-safe.
- Config does not store private keys.
- Security key files remain local-only and must not appear in CLI output, logs, docs examples, or tests except artificial field-name checks. On Windows, local signing, exchange, and storage secret bytes should be wrapped with current-user DPAPI fields, and older plaintext-hex key files should migrate during security-state ensure. On macOS/Linux with a native store available, older plaintext-hex key and relay credential files should migrate to OS-secret references. On non-Windows with a configured user-managed wrap key and no native store, older plaintext-hex files should migrate to encrypted wrapped fields; without native storage or a wrap key, files remain owner-only local secrets.

## Replay And Signatures

- Local agent cards are signed and signature verification fails on tampering.
- Remote signed agent-card imports reject tampered cards and cross-peer agent-id collisions.
- Replay cache rejects duplicate message request and envelope ids before duplicate delivery.
- Revoked peers must not remain visible or routeable.

## Routes

- Direct route candidates are recorded only for trusted peers and must be selected only after a live authenticated QUIC probe succeeds; failed probes and NAT-traversal-unavailable states must keep relay selected.
- Relay fallback does not weaken trust checks.
- Direct endpoint config must not contain tokens, private keys, endpoint secrets, or payload material, and rejected endpoint strings must not be persisted in route ids, route registries, probes, logs, CLI output, SDK output, or MCP output.
- Route failure reasons stay generic and must not echo arbitrary payload-bearing input.

## Packaging And Doctor

- Release archives do not include local state, private keys, logs, inboxes, message stores, routes, or payload-bearing files.
- `conu doctor` reports readiness and scan counts only; it must not print log contents.
- `conu logs rotate` reports only metadata such as file names, byte sizes, rotation counts, and archive removals. It must not read, print, upload, or classify log contents.
- `conu telemetry snapshot` reports only schema, explicit allowlist, aggregate counters, and display guards. It must not print node ids, agent ids, peer ids, endpoints, file paths, log lines, key ids, private keys, shared secrets, auth tokens, plaintext payloads, decrypted payloads, or ciphertext bodies.
- `conu update check` reports public release-policy metadata, checksum/signature sidecar status, optional GPG verification status, asset counts, and false display guards only. Local mode reads a policy plus sidecars from disk; remote mode may fetch only the public policy JSON, strict `.sha256` sidecar, and detached `.asc` sidecar over HTTPS with TLS, public-host, timeout, size, and redirect limits. It must not download update archives, apply updates, print detached-signature contents, print GPG verifier output, display private keys, tokens, local conU state, payloads, ciphertext bodies, or enable downgrade/auto-apply behavior. `conu update download` may revalidate the policy and download only one selected public platform archive plus strict checksum and detached-signature sidecars into an operator-chosen output directory; it must verify the archive SHA-256 against policy metadata and sidecar, optionally verify the artifact signature with GPG without printing verifier output, fail before writing on checksum/signature validation errors, and avoid clobbering existing output files. `conu update apply` may revalidate that policy, verify an already-downloaded archive and sidecars, reject cross-target application, scan only bounded safe archive members, stage only the four expected conU binaries, report dry-run install plans without writing, and require `--confirm` before replacing binaries with backups, temp-file cleanup on failure, and backup restoration after replacement failure. It must not execute downloaded binaries, print archive member contents, detached-signature contents, GPG output, private keys, tokens, local conU state, payloads, ciphertext bodies, or enable unattended downgrade/auto-apply behavior.
- `conu security rotate storage --confirm` reports only key ids and migration counts. It must not print key bytes, DPAPI blobs, plaintext payloads, or decrypted payloads.
- `conu security rotate identity --confirm-peer-refresh` and `conu security retire identity --confirm-peer-refresh-complete` report only key ids, archive counts, refresh/confirmation booleans, compatibility status, and `contentsDisplayed=false`. They must not print private keys, DPAPI blobs, shared secrets, plaintext payloads, or decrypted payloads.
- `conu security retire storage --confirm` reports only archived-key, migrated-file, and dependent-file counts. It must not print key bytes, DPAPI blobs, plaintext payloads, or decrypted payloads.
- CI and release workflows upload binaries/docs/templates only.
- Live tagged-release readiness audits may report required secret names, missing variable names, public readiness booleans, release tag status, and package names only. They must not print GitHub secret values, repository variable values, release notes, asset download URLs, tokens, token hashes, signing material, local conU state, payloads, ciphertext bodies, or file contents.
- Release workflows must verify archives before upload with bounded streaming inspection and strict checksum files that name the matching archive, require install/service templates in each archive, reject local conU state, private keys, logs, inboxes, route registries, telemetry dumps, node modules, vendored npm binaries, and payload-bearing files, require release artifact smoke preflights to reject missing/non-file binary paths before execution, require package-manager manifest generation regressions with APT/RPM metadata validation plus native RPM spec and RPM asset preflight where RPM tooling is available, require package-manager submission bundle regressions, require release update-policy regressions, require release update-download/apply dry-run gate regressions, require hosted Linux repository endpoint regression checks, require hosted Linux repository S3 publication regression checks, require Linux signing-secret preflight, RPM package-signing, fingerprint-pinned Linux detached-signing, fingerprint-pinned repository-signing, hosted Linux repository bundle and site, GitHub Pages readiness, GitHub Release clobber, GitHub Release asset publication, and fingerprint-pinned public-key-export regression checks with ephemeral key material, require npm installer extraction to reject excessive member counts, duplicate normalized paths, and forbidden local state paths before extraction, bound extracted-tree scanning, select binaries only from the expected release root `bin/` directory without duplicate binary names elsewhere, and run npm launcher local-smoke preflight regression checks for missing/non-file binary fixtures, sign Windows binaries, sign/notarize macOS ZIP archives, verify the imported Linux signing key against `CONU_LINUX_GPG_KEY_FINGERPRINT`, fail tagged releases early when Linux signing secrets cannot import, fingerprint-match, probe-sign with the configured passphrase, when the GitHub Release tag is already published, when default Pages repository metadata is not ready, or when custom S3 publication settings are missing for a configured custom repository base URL, sign generated RPM packages and refresh `.rpm.sha256` sidecars before RPM repository metadata generation, export `conu-linux-gpg-key.asc` plus its `.sha256` sidecar without private-key material, add native APT/RPM repository metadata signatures from maintainer release secrets, generate detached GPG `.asc` signatures for Linux archives and generated Linux package/metadata assets from maintainer release secrets, generate and detached-sign package-manager submission bundles, hosted Linux repository bundles, and site artifacts from already signed package/repository assets, publish a verified custom repository site to a configured S3-compatible target with generated cache headers and live endpoint readiness when requested, generate and detached-sign release update-policy metadata from the final public asset set with auto-apply disabled, verify the published public-key fingerprint and run public `conu update check`, `conu update download`, and `conu update apply --dry-run` with GPG verification after upload, verify public GitHub Release asset metadata before npm registry access, generate GitHub artifact attestations for release archives plus checksum files, generate APT repository metadata only from generated `.deb` assets, generate RPM repository metadata only from signed generated `.rpm` assets, and build RPM release assets only from verified Linux archives. Signing, release-clobber checks, release-asset metadata checks, package-manager submission reports, S3 publication reports, endpoint readiness reports, update-policy checks, update download/apply gates, and notarization logs must not display certificates, private keys, signing passwords, GPG private keys, GPG passphrases, npm tokens, relay tokens, local conU state, or payload contents.
- Service templates must not bake in developer-specific secrets, tokens, or private paths beyond editable placeholders.
- npm packaging must verify release checksums by default with strict archive-name matching and streamed archive hashing, require trusted download URLs, enforce bounded archive/checksum downloads, preflight local `CONU_NPM_BINARY_DIR` overrides as existing directories with regular files for every expected binary before vendor writes, run deterministic package-content verification before publication, refuse normal tagged GitHub Release publication when the release already exists for the tag, run the GitHub Release asset publication preflight before npm registry access, run the npm publish preflight before tagged publication so duplicate target versions fail before any partial publish, and must not package or inspect local `CONU_HOME` state or print npm token values.
- Generated Homebrew/Scoop/winget/Chocolatey/Debian/RPM package-manager outputs, package-manager submission bundles, APT/RPM repository metadata, hosted Linux repository bundles, hosted Linux repository site artifacts, release update-policy metadata, and GitHub Pages deployment artifacts must be derived from verified release assets and strict checksum files. Manifest/spec outputs must contain only public release URLs, static SHA-256 hashes, package metadata, install helper code, and binary mappings; generated Debian/RPM packages may embed only verified Linux release binaries plus minimal package metadata/docs; generated APT metadata may contain only hashes/sizes for the generated `.deb` assets plus repository metadata and native `InRelease`/`Release.gpg` signatures after tagged signing; generated RPM repository metadata may contain only `createrepo_c` metadata for signed generated `.rpm` assets plus `repodata/repomd.xml.asc` after tagged signing, without embedding RPM payloads; package-manager submission bundles may contain only generated repository-ready package-manager files, signed Linux package/metadata files, public key material, public signatures, sidecars, and false display guards; hosted repository bundles may contain only public package files, public key material, public signatures, sidecars, and repository metadata; hosted repository site and Pages artifacts may contain only public repository files, endpoint metadata, install snippets, public cache policy files, public signatures/checksums, and signed bundle downloads; update-policy metadata may contain only public release URLs, static SHA-256 hashes, signature URLs, npm package versions, manual verification rules, and false display guards with auto-apply disabled. Native package checks may build temporary RPMs from verified fixture archives and generated specs, and tagged releases may build RPM assets from the verified Linux archives, verify the imported signing key fingerprint in tag preflight and again during publication, probe-sign before builds, verify default Pages repository metadata when `CONU_LINUX_REPOSITORY_BASE_URL` is unset, sign the RPM package payloads, refresh `.rpm.sha256` sidecars, generate RPM repository metadata from the signed packages, export `conu-linux-gpg-key.asc` plus a strict `.sha256` sidecar containing only armored public-key material, add native repository metadata signatures, refresh sidecars, attach detached `.asc` signatures for Linux archives and generated Linux package/metadata assets, then generate and detached-sign the package-manager submission bundle, hosted Linux repository bundle, site artifact, and release update-policy metadata. Before Pages deployment, the site ZIP must be checksum/signature preflighted and safely extracted from verified paths only, cache policy files must be validated, and repository Pages metadata must be checked without reading or printing secrets. Before custom S3 publication, every extracted site file must be covered by exactly one generated cache rule, S3 bucket/prefix/endpoint values must not contain credentials, and upload commands may pass only public file paths, S3 object paths, content types, and cache headers to the AWS CLI. After a custom hosted repository endpoint is published, endpoint readiness may fetch public `repository.json`, `cache-policy.json`, `_headers`, public key, repository metadata, and signed bundle URL headers only to verify live `Cache-Control`; it must not fetch or print secrets, local paths, private payloads, tokens, or signing material. Before npm registry access, public GitHub Release metadata must be checked for the expected uploaded asset names, positive asset sizes, duplicate names, draft/tag mismatches, and forbidden state/secret-looking names without reading asset bodies or printing download URLs. Those packages, package-manager submission bundles, metadata bundles, hosted repository bundles, hosted repository site artifacts, update-policy metadata, Pages artifacts, S3 publication reports, native repository signatures, detached signatures, release-asset metadata reports, endpoint readiness reports, and the public-key asset must not include secrets, local state, logs, inboxes, route registries, private payloads, or repository credentials. They must not include maintainer secrets, npm tokens, relay tokens, token hashes, signing material, private keys, local filesystem paths beyond local input file arguments, conU state paths, private payload fields, unrelated archive contents, or package-manager repository credentials.
- Docker relay templates must keep relay tokens in runtime configuration or explicit issued token files only; credential manifests may store token hashes and lifecycle metadata, but never raw relay tokens, and examples should prefer `--credentials-file`, `--replace`, and `--revoke-credential` over hand-editing where possible.

## CLI Watch

- Shows route, latency, bytes, packet count, stream count, presence.
- Never shows message text, prompt text, reasoning, file contents, or tool output.

## MCP Adapter

- stdout contains only valid MCP JSON-RPC messages.
- Tool schemas do not encourage plaintext payload logging.
- `conu_receive_message` returns metadata by default.
- `payloadHex` is returned only for explicit addressed-agent receive calls.
- When `CONU_AGENT_ID` is set, the MCP server rejects attempts to act as another local agent.
